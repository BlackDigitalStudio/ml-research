# archive — historical layers, moved as-is (2026-07-11 cleanup)

Nothing in here is maintained or safe to run. It is kept for provenance: ledger and
RESEARCH_LOG records reference these files and their results. Original paths are
preserved in git history (`git log --follow`).

Contents: the pre-HD live-bot stack (`src/`, `main.py`, `systemd/`, `tests/`,
`requirements*.txt`), closed research tiers (`scripts/` — hd1_*/hd2_*/ha*/grid_*/
phase_b/stacker/SSL/RL/mamba families), old run outputs (`runs/`, `research_runs/`,
`catboost_info/`, `modal_bakeoff/`, `tools/`, `docs/`), and superseded docs
(`STRATEGY.md`, `mamba2_arch.md`, `Самые важные метрики`).

Do NOT extend anything here. If a number from these files matters, re-verify it
against the ledger and current data first (CLAUDE.md "Interpreting records" rules).
