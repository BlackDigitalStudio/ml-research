---
name: scope-bound-claims
description: "READ FIRST when recalling any quantitative conclusion from this memory: every number here is a MEASURED CELL (symbol × year × execution × features × protocol), not a law; re-measure before applying to any other model/feature-set/signal/market"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 6287b103-b1f8-4152-bb04-df2d7ec2e6cf
---

**Every quantitative conclusion in this memory is bound to the exact cell where it was measured.** The user has watched agents over-generalize for several sessions running (e.g. citing "always-last in queue radically changes the picture" from noA results — a policy NOT used in deploy; the qm0/qm1 ranking flip is a measured conditional of one sim on one dataset, HD3 rev5).

**Why:** compression strips conditions; recall reads one index line and pattern-fills the rest; a decisive-sounding claim beats a conditional one in an agent's head. This is the cross-session twin of the failure documented in CLAUDE.md ("collapsing the exploratory map into a verdict").

**How to apply:**
- When recalling any number (e.g. "~−3bp maker floor"), restate its cell out loud first: DOGE-USDT CL-year 2025-05→2026-06, AxB/noA xgb-71-feat family, always-last both legs, 12.8s entry / 30s-hold pegged maker exit. Outside that cell — it's a hypothesis to measure, not a fact.
- Never carry a conclusion to: other symbols, other feature sets, other signals, taker execution, other markets (stocks/forex), other horizons — without a fresh measurement.
- noA is NOT used in deploy; do not cite noA-derived conclusions as if they constrain the deployed AxB.
- Year-EV numbers from tail-selection walk-forwards are distributions (±3-5bp under seed/label perturbation, see [[label-matching-lookahead]]) — never cite a single draw as a point estimate.
